# Node TODO API

Express-based REST + static site sample highlighting modular project structure.

## Install & run

```bash
npm install
npm run dev
```

Visit `http://localhost:4000` for the landing page or target the JSON endpoints at `/todos`.

## Run tests

```bash
npm test
```

Tests exercise the CRUD flow via Supertest + Vitest.
