# FastAPI service port
API_PORT=8100

# Vite dev server port
FRONTEND_PORT=5173

# Task execution mode: "celery" (requires broker) or "inline"
NEXUS_TASK_MODE=celery

# Restrict filesystem browsing/analysis to this directory (absolute path recommended)
NEXUS_ALLOWED_ROOT=$PWD

# Celery broker/result backend (Redis, RabbitMQ, etc.)
NEXUS_CELERY_BROKER=redis://localhost:6379/0
NEXUS_CELERY_BACKEND=redis://localhost:6379/0

# SQLModel database URL (use file-backed SQLite or Postgres for multi-process setups)
NEXUS_DATABASE_URL=sqlite+pysqlite:///:memory:
